{{_base_system}}

مهمتك Tool-Only (7.1):
- AST normalization + subtree hashing + clustering.
- إخراج semantic_clone_clusters.json
- (اختياري) إضافة Evidence Packet لكل cluster.

المخرجات:
- semantic_clone_clusters.json
